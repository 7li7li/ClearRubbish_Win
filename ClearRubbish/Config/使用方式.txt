WeChatConfig微信清理配置
QQConfig QQ清理配置
以上配置保持默认会清理所有QQ和微信垃圾文件（包含图片视频等）（不包含聊天记录，可选）

Config程序配置
Path自定义目录

设置定时任务
win server:
开始菜单>管理工具>任务计划程序>创建基本任务(按需求每周或者每个月执行该程序即可)
注意:一定要在属性中设置 不管用户是否登陆都要运行

请将这些配置文件复制到程序同一个文件夹